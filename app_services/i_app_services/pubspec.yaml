name: i_app_services
description: "Хранит в себе все интерфейсы для реализации общих сервисов"
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: 6.0.0
